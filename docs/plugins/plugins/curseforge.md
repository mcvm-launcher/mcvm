# CurseForge
ID: `curseforge`

The CurseForge plugin will add the `curse` package repository, allowing you to install CurseForge projects as packages on your instances and templates.

## Usage
Browse packages under the CurseForge repository or add them manually in your config by putting `curse:` before the package ID.
