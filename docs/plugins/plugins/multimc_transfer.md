# MultiMC Transfer
ID: `multimc_transfer`

The MultiMC Transfer plugin is used to import instances from MultiMC and PrismLauncher.

## Usage
Use `multimc` and `prism` formats with [instance transfer](../../instance_transfer.md).
