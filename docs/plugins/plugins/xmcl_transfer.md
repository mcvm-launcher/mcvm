# XMCL Transfer
ID: `xmcl_transfer`

The XMCL Transfer plugin is used to import and export instances to the X Minecraft Launcher.

## Usage
Use the `xmcl` format with [instance transfer](../../instance_transfer.md).
