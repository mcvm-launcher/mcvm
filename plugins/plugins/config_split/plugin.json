{
	"name": "Config Split",
	"description": "Split configuration into multiple files",
	"protocol_version": 3,
	"hooks": {
		"add_instances": {
			"executable": "${PLUGIN_DIR}/mcvm_plugin_config_split"
		},
		"add_profiles": {
			"executable": "${PLUGIN_DIR}/mcvm_plugin_config_split"
		}
	}
}